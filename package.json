{
  "private": true,
  "workspaces": [
    "web-rtc-server",
    "web-rtc-client",
    "web-rtc-types"
  ],
  "scripts": {
    "client:dev": "yarn workspace web-rtc-client run start",
    "server:dev": "yarn workspace web-rtc-server run start:dev"
  },
  "version": "1.0.0",
  "name": "web-rtc"
}