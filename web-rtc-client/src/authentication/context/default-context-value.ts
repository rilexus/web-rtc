import { AuthenticationType } from "../types/Authentication.type";

const defaultAuthentication: AuthenticationType = {
  isLoggedIn: false,
  key: "",
  username: "",
};
export { defaultAuthentication };
